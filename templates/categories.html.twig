{# templates/categories.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}Category list{% endblock %}
{% block content %}
    <ul>    
    {% for cat in categories %}
		<li><a href = "{{ path('products', {'id': cat.codCat})}}">{{ cat.name }} </a></li> 
   {% endfor %}
    </ul>
{% endblock %}
